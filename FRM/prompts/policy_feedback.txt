We trained a RL policy using the provided reward function code and tracked the values of the individual components in the reward function as well as global policy metrics such as task scores after every {episode_freq} episodes and the maximum, mean, minimum values encountered:
